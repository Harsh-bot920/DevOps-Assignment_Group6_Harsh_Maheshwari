# Grafana Configuration

Important areas include server settings, authentication, data sources, users/permissions, dashboard provisioning and alerting. Never commit passwords, API keys or other secrets.
