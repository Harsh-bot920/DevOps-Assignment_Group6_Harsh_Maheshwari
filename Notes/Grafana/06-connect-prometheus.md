# Connect Grafana with Prometheus

```text
Exporter/Application → Prometheus → PromQL → Grafana
```

If a panel shows no data, first check the Prometheus target status, metric name, labels, data source URL and dashboard time range.
