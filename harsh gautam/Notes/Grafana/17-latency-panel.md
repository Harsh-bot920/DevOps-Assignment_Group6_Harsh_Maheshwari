# Latency Panel

Latency is the time required to process a request or operation.

Use consistent units, preferably percentiles for user-facing latency, and compare latency with traffic and error rate during incidents.
